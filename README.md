# Code Analyzer Elixir-only fixture

This repository verifies that a repository containing only recognized but
unsupported Elixir source is rejected as unsupported rather than empty.

<!-- Final live validation after the terminal preflight persistence fix. -->
